---
title: compose
source: feishu
space: docker
---

compose
通过一个yml文件，定义一组和关联的应用容器为一个项目
命令
docker compose up :启用
docker compose up -d :启用并后台运行
docker compose down：删除
docker compose ps
docker compose config：检查配置
docker compose config -q：检查配置，有问题才输出

